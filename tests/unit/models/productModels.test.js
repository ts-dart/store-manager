const { expect } = require('chai');

describe('Testa se pega todos os produtos', () => { });
describe('Testa se pega um produco com um id correspondente', () => { });
describe('Testa se e possivel inserit um novo produto', () => { });